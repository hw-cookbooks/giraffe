#The default dashboards.js file is configured and written by the giraffe_instance LWRP in the Giraffe cookbook, however you may specify your own template such as this one.

var graphite_url = "<%= @graphite_url %>";

var dashboards = <%= @dashboards %>;